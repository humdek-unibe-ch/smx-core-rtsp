###############################################################################
# CUTOMIZE THIS FILE TO SUIT YOUR NEEDS FOR THE BUILD PROCESS                 #
# This file is included into the following files (refer to them for more      #
# information on the build process):                                          #
#  - Makefile                                                                 #
#  - test/Makefile                                                            #
###############################################################################

# The version number of the box library ($(VMAJ).$(VMIN).$(VREV))
VMAJ = 0
VMIN = 1
VREV = 0
VDEB = 1

# the version of the dash frontend
DASH_VERSION = <smx_dash_version>

# additional schema defintions used for event autocompletion and schema checking
#SCHEMA_DEFINITIONS = "smx_uep_enhanced_input:smxuepenhancedinput:0.1,smx_uep_messaging:smxuepmessaging:1.1,smx_uep_xr_vive_gaze:smxuepxrvive:0.1"
SCHEMA_DEFINITIONS =

# the compiler
SMXC = <smxc_dependency>

# the graph post-processor
SMXRTSP = <smxrtsp_dependency>

# the RTS library
RTS_LIB = <smxrts_dependency>

# list of Streamix libraries to be linked and included
SMX_LIBS = <smxzlog_dependency><app_dependencies>

# list the external libraries prefixed with -l
EXT_LIBS =

# list the external libraries directories prefixed with -L
EXT_LIBS_DIR =

# list the external include files prefixed with -I
EXT_INCS_DIR =

# define additional CFLAGS
EXT_CFLAGS =

# the name of the application
APPNAME = <app_name>
